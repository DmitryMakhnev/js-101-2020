
const sum = require('./sum');
const pow = require('./modules/pow').pow;
const data = require('./data.json');
const cl = require('cl').cl;

// console.log(sum(1, 2));
// console.log(pow(2, 2));
// console.log(data.map(item => item.id));
cl('asd');
